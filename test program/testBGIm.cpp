//#include "graphics.h"
#include <graphics.h>

#include <conio.h>

int main(void)
{
    initwindow(100, 100, "First Sample");
    /*circle(100, 50, 40);
    moveto(0, 100);
    lineto(400, 300);
    moveto(0, 300);
    lineto(400, 100);
    while (!kbhit())
        delay(200);*/
    closegraph();
    return 0;
}

